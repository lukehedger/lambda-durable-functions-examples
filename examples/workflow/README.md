# workflow

An example showing how to implement a workflow with branching logic using Lambda durable functions. The strategy pattern and child contexts are used to implement the workflow.

## Deployment

Install dependencies:

```sh
bun install
```

Deploy example:

```sh
bun run cdk deploy
```
